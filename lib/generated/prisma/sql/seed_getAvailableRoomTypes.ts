
/* !!! This is code generated by Prisma. Do not edit directly. !!! */
/* eslint-disable */
// biome-ignore-all lint: generated file
// @ts-nocheck 
import * as $runtime from "@prisma/client/runtime/client"

/**
 * @param checkInDate
 * @param checkOutDate
 * @param numAdults
 * @param numChildren
 * @param numRooms
 * @param pageSize
 * @param lastHotelId (pass NULL for first page)
 */
export const seed_getAvailableRoomTypes = $runtime.makeTypedQueryFactory("\nSELECT\nrt.id,\nrt.name,\nrt.price\nFROM room_types rt\nJOIN room_type_inventories rti\nON rti.room_type_id = rt.id\nAND rti.date >= $1::date\nAND rti.date < $2::date\nWHERE ($7::uuid IS NULL OR rt.id > $7::uuid)\nAND rt.adult_capacity * $5 >= $3\nAND (rt.adult_capacity + rt.children_capacity) * $5 >= $3 + $4\nGROUP BY rt.id, rt.name, rt.price\nHAVING\nCOUNT(DISTINCT rti.date) = ($2::date - $1::date)\nAND MIN(COALESCE(rti.total_rooms, 0) - COALESCE(rti.booked_rooms, 0)) >= $5\nORDER BY rt.id ASC\nLIMIT $6") as (checkInDate: Date, checkOutDate: Date, numAdults: number, numChildren: number, numRooms: number, pageSize: number, lastHotelId: string | null) => $runtime.TypedSql<seed_getAvailableRoomTypes.Parameters, seed_getAvailableRoomTypes.Result>

export namespace seed_getAvailableRoomTypes {
  export type Parameters = [checkInDate: Date, checkOutDate: Date, numAdults: number, numChildren: number, numRooms: number, pageSize: number, lastHotelId: string | null]
  export type Result = {
    id: string
    name: string
    price: $runtime.Decimal
  }
}
